import 'package:flutter/material.dart';

class ReciterResourcesPage extends StatelessWidget {
  final String reciterId;
  const ReciterResourcesPage({super.key, required this.reciterId});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
